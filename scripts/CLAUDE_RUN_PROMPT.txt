Read AGENT_BRIEF.md and implement all tasks.

CRITICAL: You are running non-interactively. Do NOT ask for confirmation, approval, or clarification. Do NOT use brainstorming or any skill that requires user interaction. Proceed directly to implementation. If anything is ambiguous, make a reasonable decision and document it in your commit message.

Requirements
- Keep changes scoped strictly to the brief.
- Run the project's test command and ensure it passes.
- Do not merge to main.
- Commit your work when tests pass. Do not skip committing.

Safety constraints
- Do NOT run destructive commands (rm -rf, git reset --hard, git clean -fd).

At the end, output:
- Files changed
- Commands run
- Notes / follow-on work
